apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
        - name: frontend
          image: tuannm99/podzone-frontend:staging
          imagePullPolicy: Always
          ports:
            - containerPort: 80
          resources:
            limits:
              memory: '256Mi'
              cpu: '250m'

---
apiVersion: v1
kind: Service
metadata:
  name: frontend
spec:
  selector:
    app: frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: frontend
  # namespace: staging
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: 'true'
    # cert-manager.io/cluster-issuer: 'letsencrypt-dev'
    # nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  # tls:
  #   - hosts:
  #       - podzone-ui.tuannm.uk
  #     secretName: gateway-tls
  rules:
    - host: podzone-ui.tuannm.uk
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend
                port:
                  number: 80
