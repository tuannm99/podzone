logger:
  provider: "zap" # "zap" | "slog" | "noop"
  level: "debug"
  env: "dev"

redis:
  catalog:
    uri: redis://localhost:6379/0
    provider: real

mongo:
  catalog:
    uri: mongodb://localhost:27017/catalog
    provider: real

grpc:
  port: 50052
