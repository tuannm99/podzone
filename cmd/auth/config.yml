logger:
  app_name: "podzone_admin_auth"
  provider: "zap" # "zap" | "slog"
  level: "debug"
  env: "dev"

redis:
  auth:
    uri: redis://localhost:6379/0
    provider: real

sql:
  auth:
    uri: postgres://postgres:postgres@localhost:5432/auth?sslmode=disable
    provider: postgres

grpc:
  port: 50051
